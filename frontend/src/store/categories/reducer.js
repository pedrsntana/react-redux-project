import { actionTypes } from './actions';

const getInitialState = () => ({
  postCategories: [],
  deleted: false,
  isFetching: false,
  error: '',
});

const categories = (state = getInitialState(), {type, payload}) => {
  switch (type) {
    case expression:

      break;
    default:

  }
}
